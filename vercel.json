{
  "buildCommand": "PORT=22333 BASE_PATH=/ pnpm --filter @workspace/web run build && rm -rf public && mkdir -p public && cp -R artifacts/web/dist/public/. public/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}